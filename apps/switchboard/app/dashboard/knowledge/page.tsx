import { DemoButton } from "@/components/demo-actions";
import { Card, MetricCard, PageHeader, StatusBadge } from "@/components/ui";
import { knowledgeBases } from "@/lib/mock-data";

export default function KnowledgePage() {
  return <div className="page"><PageHeader eyebrow="Build" title="Knowledge Bases" description="Govern document sources and retrieval boundaries for AI Applications." action={<DemoButton action="Opened add knowledge source flow" target="Knowledge Bases">Add source</DemoButton>} /><div className="grid kpis"><MetricCard label="Knowledge bases" value={String(knowledgeBases.length)} detail="Connected sources" status="Healthy" /><MetricCard label="Documents indexed" value={String(knowledgeBases.reduce((sum, kb) => sum + kb.documents, 0))} detail="Across sources" status="Healthy" /><MetricCard label="Restricted sources" value={String(knowledgeBases.filter((kb) => kb.sensitivity === "Restricted").length)} detail="Explicit access required" status="Warning" /><MetricCard label="Sync status" value="1 syncing" detail="No failed indexes" status="Warning" /></div><Card style={{ marginTop: 18 }}><div className="card-header"><div><h3>Source registry</h3><p className="muted">Retrieval requires application access, team access, and chunk sensitivity clearance.</p></div></div><div className="table-wrap"><table><thead><tr><th>Knowledge base</th><th>Source</th><th>Documents</th><th>Sensitivity</th><th>Applications</th><th>Teams</th><th>Last sync</th><th>Status</th></tr></thead><tbody>{knowledgeBases.map((kb) => <tr key={kb.id}><td><strong>{kb.name}</strong></td><td>{kb.source}</td><td>{kb.documents}</td><td>{kb.sensitivity}</td><td>{kb.assignedApps.join(", ")}</td><td>{kb.assignedTeams.join(", ")}</td><td>{kb.lastSync}</td><td><StatusBadge value={kb.status} /></td></tr>)}</tbody></table></div></Card></div>;
}
