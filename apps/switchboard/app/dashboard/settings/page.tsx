import { Card, MetricCard, PageHeader, StatusBadge } from "@/components/ui";
import { organization } from "@/lib/mock-data";

export default function SettingsPage() {
  return <div className="page"><PageHeader eyebrow="System" title="Settings" description="Organisation profile, identity provider, alert thresholds, integrations, retention, and governance defaults." action={<button className="button">Save changes</button>} /><div className="grid kpis"><MetricCard label="Organisation" value={organization.name} detail={organization.domain} status="Healthy" /><MetricCard label="Data region" value={organization.region} detail="Customer-selected residency" status="Healthy" /><MetricCard label="Default chat" value="AnythingLLM" detail="Deployed per application" status="Healthy" /><MetricCard label="Audit retention" value="7 years" detail="Append-only events" status="Healthy" /></div><div className="grid two" style={{ marginTop: 18 }}><Card pad><h3>Alert thresholds</h3><p className="muted">GPU memory warning 85%, critical 95%. Latency warning 1200 ms. Monthly cost warning 80% of budget.</p><StatusBadge value="Active" /></Card><Card pad><h3>Integrations</h3><p className="muted">SSO: Microsoft Entra ID. Notifications: Slack and email webhook. Secrets are represented by references only.</p><StatusBadge value="Connected" /></Card><Card pad><h3>Agent installer</h3><p className="muted">Registration tokens are one-time and expire on first connection. Commands remain typed and allowlisted.</p><StatusBadge value="Healthy" /></Card><Card pad><h3>Governance disclaimer</h3><p className="muted">Evidence readiness supports ISO/IEC 42001 preparation. It does not mean certification.</p><StatusBadge value="Required" /></Card></div></div>;
}
