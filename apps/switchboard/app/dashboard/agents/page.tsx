import { ActionButton } from "@/components/action-button";
import { Card, MetricCard, PageHeader, StatusBadge } from "@/components/ui";
import { agents } from "@/lib/mock-data";
import { aed } from "@/lib/utils";

export default function AgentsPage() {
  return <div className="page"><PageHeader eyebrow="Build" title="Governed Agents" description="AI workers with fixed model, knowledge, tool, approval, budget, and kill-switch boundaries." action={<ActionButton action="Opened create governed agent flow" target="Governed Agents">Create agent</ActionButton>} /><div className="grid kpis"><MetricCard label="Agents" value={String(agents.length)} detail="Configured workers" status="Healthy" /><MetricCard label="Approval rules" value={String(agents.filter((a) => a.approvalRule.includes("Approval")).length)} detail="Human oversight" status="Healthy" /><MetricCard label="External blocked" value={String(agents.filter((a) => a.externalModelRule === "Blocked").length)} detail="Local-only workers" status="Healthy" /><MetricCard label="Kill switches" value="Ready" detail="Operator controlled" status="Healthy" /></div><Card style={{ marginTop: 18 }}><div className="card-header"><div><h3>Agent registry</h3><p className="muted">External actions pause for approval with exact payload review.</p></div></div><div className="table-wrap"><table><thead><tr><th>Agent</th><th>Owner</th><th>Models</th><th>Knowledge</th><th>Tools</th><th>Approval</th><th>Budget</th><th>Status</th></tr></thead><tbody>{agents.map((agent) => <tr key={agent.id}><td><strong>{agent.name}</strong></td><td>{agent.owner}</td><td>{agent.allowedModels.join(", ")}</td><td>{agent.knowledgeBases.join(", ")}</td><td>{agent.tools.join(", ")}</td><td>{agent.approvalRule}</td><td>{aed(agent.budgetAed)}</td><td><StatusBadge value={agent.status} /></td></tr>)}</tbody></table></div></Card></div>;
}
