import { ButtonLink, Card, MetricCard, PageHeader, StatusBadge } from "@/components/ui";
import { modelCatalog } from "@/lib/mock-data";
import { aed } from "@/lib/utils";

export default function ModelCatalogPage() {
  return <div className="page"><PageHeader eyebrow="Control" title="Model Catalog" description="The governed source of truth for local and external models assignable to AI Applications." action={<button className="button">Add model</button>} /><div className="grid kpis"><MetricCard label="Catalog models" value={String(modelCatalog.length)} detail="Local and external" status="Healthy" /><MetricCard label="Local models" value={String(modelCatalog.filter((m) => m.hosting === "Customer server").length)} detail="Customer infrastructure" status="Healthy" /><MetricCard label="External providers" value={String(modelCatalog.filter((m) => m.hosting === "External provider").length)} detail="Secret references only" status="Warning" /><MetricCard label="Fallback-ready" value={String(modelCatalog.filter((m) => m.fallbackEligible).length)} detail="Eligible routes" status="Healthy" /></div><Card style={{ marginTop: 18 }}><div className="card-header"><div><h3>Governed model catalog</h3><p className="muted">Only Running or Connected models can be selected by applications.</p></div><ButtonLink href="/dashboard/safeguards" secondary>Review routing</ButtonLink></div><div className="table-wrap"><table><thead><tr><th>Model</th><th>Hosting</th><th>Runtime</th><th>Sensitivity</th><th>Target</th><th>Cost / 1M</th><th>Status</th></tr></thead><tbody>{modelCatalog.map((model) => <tr key={model.id}><td><strong>{model.name}</strong><br /><span className="muted">{model.provider}</span></td><td>{model.hosting}</td><td>{model.runtime}</td><td>{model.sensitivityFit}</td><td>{model.target}</td><td>{model.inputCostAed ? `${aed(model.inputCostAed)} in / ${aed(model.outputCostAed)} out` : "Local capacity"}</td><td><StatusBadge value={model.status} /></td></tr>)}</tbody></table></div></Card></div>;
}
