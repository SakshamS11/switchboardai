import { Card, MetricCard, PageHeader, StatusBadge } from "@/components/ui";
import { routingPolicies } from "@/lib/mock-data";

export default function SafeguardsPage() {
  return <div className="page"><PageHeader eyebrow="Control" title="Safeguards & Routing" description="Sovereignty, cost, provider degradation, circuit breaker, and prompt firewall policies." action={<button className="button">Create policy</button>} /><div className="grid kpis"><MetricCard label="Policies" value={String(routingPolicies.length)} detail="Versioned controls" status="Healthy" /><MetricCard label="Sovereignty routers" value={String(routingPolicies.filter((p) => p.module === "Sovereignty Router").length)} detail="Fail closed for restricted data" status="Healthy" /><MetricCard label="Provider drift" value="OpenAI warning" detail="Fallback route armed" status="Warning" /><MetricCard label="Blocked external" value="Claims" detail="Local-only application" status="Healthy" /></div><Card style={{ marginTop: 18 }}><div className="card-header"><div><h3>Policy registry</h3><p className="muted">Every request entering the on-server gateway is evaluated against application policy.</p></div></div><div className="table-wrap"><table><thead><tr><th>Policy</th><th>Module</th><th>Scope</th><th>Primary</th><th>Fallback</th><th>Blocked</th><th>Version</th><th>Status</th></tr></thead><tbody>{routingPolicies.map((policy) => <tr key={policy.id}><td><strong>{policy.name}</strong><br /><span className="muted">{policy.sensitivity}</span></td><td>{policy.module}</td><td>{policy.scope}</td><td>{policy.primary}</td><td>{policy.fallback}</td><td>{policy.blocked}</td><td>{policy.version}</td><td><StatusBadge value={policy.status} /></td></tr>)}</tbody></table></div></Card><Card pad style={{ marginTop: 18 }}><h3>Sovereignty Router rule</h3><p className="muted">Restricted requests fail closed. If no approved local model is available, the gateway blocks the request rather than falling back externally.</p></Card></div>;
}
