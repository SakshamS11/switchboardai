import { Card, MetricCard, PageHeader, Progress, StatusBadge } from "@/components/ui";
import { teams } from "@/lib/mock-data";
import { aed, percent } from "@/lib/utils";

export default function TeamsPage() {
  return <div className="page"><PageHeader eyebrow="Control" title="Teams & Access" description="Assign applications, model access, knowledge access, token budgets, and ownership by team." action={<button className="button">Invite user</button>} /><div className="grid kpis"><MetricCard label="Teams" value={String(teams.length)} detail="With governed access" status="Healthy" /><MetricCard label="Users" value={String(teams.reduce((sum, team) => sum + team.users, 0))} detail="Assigned to applications" status="Healthy" /><MetricCard label="At-risk teams" value={String(teams.filter((team) => team.risk !== "Healthy").length)} detail="Budget or capacity pressure" status="Warning" /><MetricCard label="Team budgets" value={aed(teams.reduce((sum, team) => sum + team.spendBudgetAed, 0))} detail="Current monthly budget" status="Healthy" /></div><div className="grid two" style={{ marginTop: 18 }}>{teams.map((team) => <Card pad key={team.id}><div className="row"><h3>{team.name}</h3><StatusBadge value={team.risk} /></div><p className="muted">Owner: {team.owner} - {team.users} users</p><p><strong>Applications:</strong> {team.applications.join(", ")}</p><div className="mini-bars"><div className="mini-bar"><span>Tokens</span><Progress value={percent(team.tokensUsed, team.tokenBudget)} /><span>{percent(team.tokensUsed, team.tokenBudget)}%</span></div><div className="mini-bar"><span>Spend</span><Progress value={percent(team.spendUsedAed, team.spendBudgetAed)} /><span>{percent(team.spendUsedAed, team.spendBudgetAed)}%</span></div></div></Card>)}</div></div>;
}
